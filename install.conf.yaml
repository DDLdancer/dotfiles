- defaults:
        link:
                relink: true
                create: true


- clean: ['~']

- link:
        ~/.tmux.conf: tmux.conf
        ~/.vimrc: vimrc
        ~/.bashrc: bashrc
        ~/.zshrc: zshrc
        ~/.aria2/aria2.conf: aria2.conf

- create:
        - ~/downloads
        #- ~/.vim/undo-history

- shell:
         - [git submodule update --init --recursive, Installing submodules]
